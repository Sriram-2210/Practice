# Tutorial

```
My first markdown paragraph. This automatically expands or compresses the borders for the code snippet and is very usefil.
```

```python
# Example for basic python code
i = int(input('Enter value:'));
for i in range(0, i + 1):
    print(i**2/2)
```
We can also use it for different programming languages like python, javascript and many more.

```java
class main{
    public static void main(String[] args){
        int i = 0;
        for (int j = 0; j < args.length; j++) {
            if (args[j][0] == '-' && args[
                0][0] == '-' && args[1][0] == '-' && args[2][0] == '
                 ] == '-' && args[3][0] == '-' && args[4][0] == '-' && args
                   ) {
                        i += 1;
                        break;
                           }
                            else if (args[j][0] == '-' && args[1][0] == '-' && argsort
                                [0][0] == '-' && argsort[0][0] == '
                                    ] == '-' && argsort[1][0] == '-' && argsort[
                                        2][0] == '-' && argsort[3][0] == '-' && args
                                        ] == '-' && argsort[4][0] == '-' && argsort[5
                                        ] == '-' && argsort[6][0] == '-' && argsort[7
                                        ] == '-' && argsort[8][0] == '-' && argsort[9
                                        ] == '-' && argsort[10][0] == '-' && argsort[11
                                        ] == '-' && argsort[12][0] == '-' && argsort[13
                                        ] == '-' && argsort[14][0] == '-' && argsort[15
                                        ] == '-' && argsort[16][0] == '-' && argsort[17
                                        ] == '-' && argsort[18][0] == '-' && argsort[19
                                        ] == '-' && argsort[20][0] == '-' && argsort[21
                                        ] == '-' && argsort[22][0] == '-' && argsort[
                                            23][0] == '-' && argsort[24][0] == '-' && args
                                            ] == '-' && argsort[25][0] == '-' && argsort[26
                                             ] == '-' && argsort[27][0] == '-' && argsort[
                                                 ] == '-' && argsort[28][0] == '-' && argsort[
                                                    ] == '-' && argsort[29][0] == '-' && argsort[
                                                        ] == '-' && argsort[30][0] == '-' && argsort[
                                                            ] == '-' && argsort[31][0] == '-' && argsort[
                                                                ] == '-' && argsort[32][0] == '-' && argsort[
                                                                    ] == '-' && argsort[33][0] == '-' && argsort[
                                                                        ] == '-' && argsort[34][0] == '-' && argsort[
                                                                            ] == '-' && argsort[35][0] == '-' && argsort
}
```

~Hi This above example is not factually true~

**Bald Hero**

